<?xml version="1.0" encoding="utf-8"?>
<!--
  P0 spike config. Two capabilities matter here:
    canRetrieveWindowContent  -> lets us read rootInActiveWindow (the node tree)
    canPerformGestures        -> lets us dispatchGesture (taps / swipes)
  Scoped to Instagram only (packageNames) so the service sees nothing else —
  matches the V3 plan's "do nothing off-app" posture (§6.7).
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows|flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:canPerformGestures="true"
    android:notificationTimeout="100"
    android:packageNames="com.instagram.android"
    android:description="@string/a11y_description" />
